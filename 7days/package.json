{
  "name": "seven-days",
  "version": "1.0.0",
  "private": true,
  "description": "7 Days... — Capacitor wrapper for web build → Android (Play)",
  "scripts": {
    "sync:www": "node scripts/sync-www.mjs",
    "cap:sync": "npm run sync:www && npx cap sync android",
    "android:open": "npx cap open android",
    "android:bundle": "npm run cap:sync && cd android && .\\gradlew.bat bundleRelease",
    "android:apk": "npm run cap:sync && cd android && .\\gradlew.bat assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/core": "^7.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.2.0"
  }
}
